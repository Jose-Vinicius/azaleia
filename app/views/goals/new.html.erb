<% content_for :head do %>
  <script src="https://cdn.tailwindcss.com?plugins=forms,container-queries"></script>
  <link href="https://fonts.googleapis.com/css2?family=Geist:wght@400;500;600;700;800&display=swap" rel="stylesheet"/>
  <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&display=swap" rel="stylesheet"/>
  <style>
      body { font-family: 'Geist', sans-serif; }
  </style>
<% end %>

<%= render 'shared/sidebar' %>

<main class="flex-1 flex flex-col h-full w-full overflow-hidden bg-background relative">
  <header class="pt-8 md:pt-12 px-6 md:px-8 pb-6 flex-shrink-0 w-full border-b border-outline-variant/30">
    <div class="flex items-center gap-3">
      <%= link_to goals_path, class: "text-on-surface-variant hover:text-on-surface p-1 rounded-lg transition-colors" do %>
        <span class="material-symbols-outlined text-[20px]">arrow_back</span>
      <% end %>
      <h2 class="text-2xl md:text-3xl font-black text-on-surface tracking-tight">Criar Nova Meta SMART</h2>
    </div>
  </header>

  <div class="flex-1 overflow-y-auto p-6 md:p-8 hide-scrollbar max-w-4xl">
    <%= render "form", goal: @goal %>
  </div>
</main>
