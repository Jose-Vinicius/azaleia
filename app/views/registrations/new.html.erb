<% content_for :head do %>
  <script src="https://cdn.tailwindcss.com?plugins=forms,container-queries"></script>
  <link href="https://fonts.googleapis.com/css2?family=Geist:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&display=swap" rel="stylesheet">
  <script>
    tailwind.config = {
      darkMode: "class",
      theme: {
        extend: {
          colors: {
            "background": "#09090b",
            "on-background": "#fafafa",
            "surface": "#0c0c0f",
            "surface-container": "#121215",
            "surface-container-lowest": "#09090b",
            "surface-container-high": "#18181b",
            "on-surface": "#fafafa",
            "on-surface-variant": "#a1a1aa",
            "primary": "#a78bfa",
            "on-primary": "#0a0012",
            "primary-container": "#7c3aed",
            "outline-variant": "#27272a",
            "outline": "#52525b",
            "error": "#ef4444",
            "error-container": "#3b1111",
            "on-error-container": "#fca5a5",
          },
          fontFamily: {
            "body": ["Geist", "sans-serif"],
          }
        }
      }
    }
  </script>
  <style>
    body { font-family: 'Geist', sans-serif; }
  </style>
  <script>
    document.documentElement.classList.add('dark');
    document.addEventListener("DOMContentLoaded", () => {
      document.body.className = "bg-background text-on-background min-h-screen w-full flex items-center justify-center font-body antialiased";
    });
    document.addEventListener("turbo:load", () => {
      document.body.className = "bg-background text-on-background min-h-screen w-full flex items-center justify-center font-body antialiased";
    });
  </script>
<% end %>

<div class="w-full max-w-md px-6">
  <!-- Logo & Title -->
  <div class="flex flex-col items-center mb-10">
    <%= image_tag "logo-big.png", class: "w-32 h-32 object-contain" %>
    <h1 class="text-3xl font-black tracking-tighter text-on-surface">Azaleia</h1>
    <p class="text-sm text-on-surface-variant mt-1"><%= t('auth.registrations.new.title') %></p>
  </div>

  <!-- Error Messages -->
  <% if @user.errors.any? %>
    <div class="mb-6 p-4 bg-error-container text-on-error-container rounded-lg border border-error/50">
      <h2 class="text-sm font-bold mb-2"><%= t('auth.registrations.new.errors', count: @user.errors.count) %></h2>
      <ul class="list-disc pl-5 text-sm">
        <% @user.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <!-- Registration Form -->
  <div class="bg-surface-container border border-outline-variant rounded-xl p-8 shadow-2xl">
    <%= form_with model: @user, url: registration_path, class: "space-y-5" do |form| %>
      <div>
        <label class="block text-sm font-medium text-on-surface-variant mb-1.5"><%= t('auth.registrations.new.email') %></label>
        <%= form.email_field :email_address,
          required: true,
          autofocus: true,
          autocomplete: "email",
          placeholder: t('auth.registrations.new.email_placeholder'),
          class: "w-full bg-surface-container-lowest border border-outline-variant rounded-lg px-4 py-3 text-on-surface focus:ring-2 focus:ring-primary focus:border-primary outline-none transition-all placeholder-outline text-sm" %>
      </div>

      <div>
        <label class="block text-sm font-medium text-on-surface-variant mb-1.5"><%= t('auth.registrations.new.password') %></label>
        <%= form.password_field :password,
          required: true,
          autocomplete: "new-password",
          placeholder: t('auth.registrations.new.password_placeholder'),
          class: "w-full bg-surface-container-lowest border border-outline-variant rounded-lg px-4 py-3 text-on-surface focus:ring-2 focus:ring-primary focus:border-primary outline-none transition-all placeholder-outline text-sm" %>
      </div>

      <div>
        <label class="block text-sm font-medium text-on-surface-variant mb-1.5"><%= t('auth.registrations.new.password_confirmation') %></label>
        <%= form.password_field :password_confirmation,
          required: true,
          autocomplete: "new-password",
          placeholder: t('auth.registrations.new.password_confirmation_placeholder'),
          class: "w-full bg-surface-container-lowest border border-outline-variant rounded-lg px-4 py-3 text-on-surface focus:ring-2 focus:ring-primary focus:border-primary outline-none transition-all placeholder-outline text-sm" %>
      </div>

      <div>
        <%= form.submit t('auth.registrations.new.submit'),
          class: "w-full bg-primary text-on-primary font-bold py-3 px-4 rounded-lg hover:bg-primary/90 hover:shadow-lg transition-all cursor-pointer text-sm active:scale-[0.98]" %>
      </div>
    <% end %>
  </div>

  <!-- Links -->
  <div class="mt-6 flex flex-col items-center gap-3 text-sm">
    <div class="text-on-surface-variant">
      <%= t('auth.registrations.new.has_account') %>
      <%= link_to t('auth.registrations.new.login'), new_session_path, class: "text-primary font-semibold hover:text-primary/80 transition-colors" %>
    </div>
  </div>
</div>
